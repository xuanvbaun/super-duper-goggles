# 全局执行规则

- 不删除、覆盖用户原始数据；仅操作任务专属输出目录。
- 不执行任意 shell 命令，不使用 eval，不提供删除或密钥修改接口。
- 默认尝试两次；总控可调整输入重规划一次，再失败即停止。
- 自动检查通过后进入 REVIEW，执行器不得自行置为 DONE。
- 高风险任务仅分流至总控，不执行。
- 密钥禁止写入代码、任务或日志。第一阶段没有密钥输入功能。
- SQLite 为任务事实来源，JSON 为可重建交接副本。
