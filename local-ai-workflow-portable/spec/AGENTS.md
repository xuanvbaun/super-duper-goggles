# Codex 执行入口

先阅读 PROJECT_SPEC.md、GLOBAL_RULES.md 和 README.md。
当前范围仅规格第19节，未经用户要求不增加 AI 接入或云部署。
修改前查看现有实现与 Git diff。每阶段运行 `.venv\Scripts\python.exe -m unittest discover -s tests -v`。
不得删除用户数据或提交 .env、data、tasks、.venv。
输出本轮完成、修改文件、测试结果、未完成内容、发现的问题、下一步建议。
执行完成，等待验收。
